# Virtualized-DNS-Infrastructure
Deployed and configured a DNS filtering service inside a Linux virtual machine, implemented static IP networking, upstream DNS routing, and secure VPN-based global access using Tailscale.
